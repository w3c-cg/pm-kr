# PM-KR Reports

- `source/` contains ReSpec source documents.
- `build/` contains rendered output and should not be committed by default.
- Start the local MCP server with `npm run mcp:start`.
- Use the bundled `pm-kr` profile to scaffold `CG-DRAFT` and `CG-FINAL` report sources.
